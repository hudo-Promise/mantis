tags:
  - MANTIS mapping rule 相关接口
summary:
  - 查询 mapping rule
description:
    查询 mapping rule
parameters:
  - name: body
    in: body
    required: true
    schema:
      id: 查询 mapping rule
      properties:
        page_num:
          type: integer
        page_size:
          type: integer


responses:
  200:
    in: body
    required: true
    schema:
      example:
        {
          "code": 200,
          "data": null,
          "msg": "Execute succeeded"
        }
