tags:
  - MANTIS case相关接口
summary:
  - 维护sw映射
description:
    维护sw映射
parameters:
  - name: body
    in: body
    required: true
    schema:
      id: 维护sw映射
      properties:
        sw_alpha:
          type: string
        sw_num:
          type: integer
        delete_flag:
          type: integer

responses:
  200:
     description: 执行成功
     example:
  500:
     description: 执行失败
     example:
