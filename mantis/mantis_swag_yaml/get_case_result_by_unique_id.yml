tags:
  - MANTIS 通用信息相关接口
summary:
  - 查询 cache case-result
description:
    查询 cache case-result  -- 仅后端排查问题用
parameters:
  - name: unique_id
    in: query
    type: string
    required: true


responses:
  200:
    in: body
    required: true
    schema:
      example:
        {
          "code": 200,
          "data": [],
          "msg": "Execute succeeded"
        }
