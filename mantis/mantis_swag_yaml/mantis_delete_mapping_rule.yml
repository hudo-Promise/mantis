tags:
  - MANTIS mapping rule 相关接口
summary:
  - 删除 mapping rule
description:
    删除 mapping rule
parameters:
  - name: body
    in: body
    required: true
    schema:
      id: 删除 mapping rule
      properties:
        id:
          type: integer


responses:
  200:
    in: body
    required: true
    schema:
      example:
        {
          "code": 200,
          "data": null,
          "msg": "Execute succeeded"
        }
