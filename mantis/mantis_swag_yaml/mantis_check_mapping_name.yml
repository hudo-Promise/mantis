tags:
  - MANTIS mapping rule 相关接口
summary:
  - 校验 mapping name
description:
    校验 mapping name
parameters:
  - name: body
    in: body
    required: true
    schema:
      id: 校验 mapping name
      properties:
        mapping_name:
          type: string


responses:
  200:
    in: body
    required: true
    schema:
      example:
        {
          "code": 200,
          "data": null,
          "msg": "Execute succeeded"
        }
